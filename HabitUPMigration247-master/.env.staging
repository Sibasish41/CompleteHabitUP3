# Staging Environment Configuration
# Copy this file and rename to .env for staging deployment
# Make sure to update all placeholder values with actual staging values

# Server Configuration
NODE_ENV=staging
PORT=5000
CLIENT_URL=https://staging.yourdomain.com

# Database Configuration (Update with staging database credentials)
DB_HOST=staging-db-host
DB_USER=staging-db-user
DB_PASSWORD=staging-secure-password
DB_NAME=habitup_staging_db
DB_PORT=3306

# JWT Configuration (Use a strong, unique secret for staging)
JWT_SECRET=your-secure-staging-jwt-secret-minimum-32-characters
JWT_EXPIRES_IN=24h

# Email Configuration (Update with staging email service)
EMAIL_HOST=smtp.your-email-provider.com
EMAIL_PORT=587
EMAIL_USER=staging@your-domain.com
EMAIL_PASS=your-staging-email-password

# Razorpay Configuration (Use test keys for staging)
RAZORPAY_KEY_ID=your-staging-razorpay-key-id
RAZORPAY_KEY_SECRET=your-staging-razorpay-key-secret

# UPI Payment Configuration (For staging/testing)
MERCHANT_UPI_ID=test-merchant@paytm
MERCHANT_NAME=HabitUP-Staging
MERCHANT_CODE=HABITUP_STG

# Support Information for UPI Payments
SUPPORT_PHONE=+91-1234567890
SUPPORT_EMAIL=staging-support@your-domain.com
SUPPORT_WHATSAPP=+91-1234567890

# File Upload Configuration
MAX_FILE_SIZE=10MB
UPLOAD_PATH=./uploads

# Rate Limiting (Moderate limits for staging)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=75

# IMPORTANT STAGING OVERRIDES:
# 1. CLIENT_URL - Must match your staging frontend URL
# 2. DB_* - All database credentials must be updated for staging
# 3. JWT_SECRET - Must be a strong, unique secret for staging
# 4. EMAIL_* - Configure with your staging email service
# 5. RAZORPAY_* - Use test/staging Razorpay keys
